import {signInUsers} from '../src/components/auth.js';

describe('signInUsers', () => {
    it.only('debería ser una función', () => {
      expect(typeof myFunction).toBe('function');
    });
  });
  